import '../css/setup.scss';

import React from 'react';

const Setup = () => (
	<div className="page">
		<h1 className="page_title">Setup</h1>
		<p>Excuse me...</p>
		<p>This page is not completed, but some options are avaliable on the game page</p>
	</div>
);

export default Setup;
